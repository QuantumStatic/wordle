from setuptools import setup
import subprocess

if __name__ == "__main__":
    # p1 = subprocess.run(["brew", "install", "enchant"],
    #                     stderr=subprocess.PIPE, text=True)
    # if p1.stderr.find("error") >= 0:
    #     print(p1.stderr)
    # raise Exception("Failed to install enchant")
    setup()
